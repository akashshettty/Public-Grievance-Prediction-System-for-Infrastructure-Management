import React from 'react'
import { motion } from 'framer-motion'
import { FileText, Download, BarChart3, Calendar } from 'lucide-react'

const Reports: React.FC = () => {
  const reportsList = [
    {
      id: 1,
      title: 'Monthly Executive Summary - May 2025',
      description: 'Comprehensive overview of infrastructure performance, risk assessment, and recommendations',
      date: 'May 30, 2025',
      type: 'Executive Report',
      size: '3.2 MB',
      icon: FileText,
    },
    {
      id: 2,
      title: 'Ward Performance Analysis Q2 2025',
      description: 'Detailed metrics for all monitored wards with comparative analysis',
      date: 'May 28, 2025',
      type: 'Analytical Report',
      size: '2.8 MB',
      icon: BarChart3,
    },
    {
      id: 3,
      title: 'Risk & Hotspot Forecast',
      description: 'AI-generated predictions for next 90 days with confidence scores',
      date: 'May 25, 2025',
      type: 'Predictive Report',
      size: '1.9 MB',
      icon: AlertTriangle,
    },
    {
      id: 4,
      title: 'Infrastructure Degradation Report',
      description: 'Detailed analysis of infrastructure health and preventive maintenance plan',
      date: 'May 20, 2025',
      type: 'Technical Report',
      size: '4.1 MB',
      icon: Shield,
    },
  ]

  const ReportIcon = ({ name }: { name: string }) => {
    if (name === 'FileText') return <FileText size={24} />
    if (name === 'BarChart3') return <BarChart3 size={24} />
    return <FileText size={24} />
  }

  return (
    <div className="space-y-12">
      {/* Header */}
      <motion.div
        initial={{ opacity: 0, y: 20 }}
        animate={{ opacity: 1, y: 0 }}
        transition={{ duration: 0.5 }}
      >
        <h1 className="text-5xl font-bold text-white mb-4">Reports & Analytics</h1>
        <p className="text-xl text-gray-300 max-w-3xl">
          Access comprehensive reports, data exports, and analytics summaries generated by the UrbanPulse AI Intelligence Platform.
        </p>
      </motion.div>

      {/* Generate Report Button */}
      <motion.div
        className="glass-premium rounded-2xl p-8 border border-white/10"
        initial={{ opacity: 0 }}
        animate={{ opacity: 1 }}
        transition={{ delay: 0.2 }}
      >
        <h3 className="text-2xl font-bold text-white mb-4">Generate Custom Report</h3>
        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">
          <input
            type="text"
            placeholder="Report title"
            className="bg-white/10 rounded-lg px-4 py-3 text-white placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-neon-cyan"
          />
          <select className="bg-white/10 rounded-lg px-4 py-3 text-white focus:outline-none focus:ring-2 focus:ring-neon-cyan">
            <option>Select Type</option>
            <option>Executive Summary</option>
            <option>Technical Analysis</option>
            <option>Risk Assessment</option>
            <option>Ward Comparison</option>
          </select>
          <input
            type="date"
            className="bg-white/10 rounded-lg px-4 py-3 text-white focus:outline-none focus:ring-2 focus:ring-neon-cyan"
          />
          <motion.button
            className="btn-primary"
            whileHover={{ scale: 1.05 }}
            whileTap={{ scale: 0.95 }}
          >
            Generate Report
          </motion.button>
        </div>
      </motion.div>

      {/* Recent Reports */}
      <motion.div
        initial={{ opacity: 0, y: 20 }}
        animate={{ opacity: 1, y: 0 }}
        transition={{ delay: 0.3 }}
      >
        <h2 className="text-3xl font-bold text-white mb-6">Recent Reports</h2>
        <div className="space-y-4">
          {reportsList.map((report, idx) => (
            <motion.div
              key={report.id}
              className="glass-premium rounded-xl p-6 border border-white/10 hover:border-neon-cyan/30 transition-colors duration-300"
              initial={{ opacity: 0, x: -20 }}
              animate={{ opacity: 1, x: 0 }}
              transition={{ delay: idx * 0.05 }}
              whileHover={{ x: 4 }}
            >
              <div className="flex items-start justify-between gap-4">
                <div className="flex items-start gap-4 flex-1">
                  <div className="w-12 h-12 rounded-lg bg-gradient-cyan/20 flex items-center justify-center text-neon-cyan flex-shrink-0">
                    <FileText size={24} />
                  </div>
                  <div className="flex-1">
                    <h3 className="text-lg font-bold text-white mb-1">{report.title}</h3>
                    <p className="text-sm text-gray-400 mb-3">{report.description}</p>
                    <div className="flex items-center gap-4 text-xs text-gray-500">
                      <span className="flex items-center gap-1">
                        <Calendar size={14} />
                        {report.date}
                      </span>
                      <span className="px-2 py-1 bg-white/10 rounded text-gray-300">{report.type}</span>
                      <span>{report.size}</span>
                    </div>
                  </div>
                </div>
                <motion.button
                  className="px-4 py-3 rounded-lg bg-white/10 hover:bg-white/20 text-white flex items-center gap-2 whitespace-nowrap transition-colors duration-300"
                  whileHover={{ scale: 1.05 }}
                  whileTap={{ scale: 0.95 }}
                >
                  <Download size={16} />
                  Download
                </motion.button>
              </div>
            </motion.div>
          ))}
        </div>
      </motion.div>

      {/* Data Export */}
      <motion.div
        className="glass-premium rounded-2xl p-8 border border-white/10"
        initial={{ opacity: 0, y: 20 }}
        animate={{ opacity: 1, y: 0 }}
        transition={{ delay: 0.5 }}
      >
        <h3 className="text-2xl font-bold text-white mb-6">Export Data</h3>
        <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
          <motion.div
            className="p-6 rounded-xl bg-white/5 border border-white/10 hover:border-neon-cyan/30 transition-colors duration-300 cursor-pointer"
            whileHover={{ scale: 1.02 }}
          >
            <p className="font-semibold text-white mb-2">📊 Complaints Data</p>
            <p className="text-sm text-gray-400 mb-4">Export all complaints with filters and formatting options</p>
            <button className="text-neon-cyan text-sm font-semibold hover:text-neon-blue transition-colors">
              Export as CSV →
            </button>
          </motion.div>
          <motion.div
            className="p-6 rounded-xl bg-white/5 border border-white/10 hover:border-neon-cyan/30 transition-colors duration-300 cursor-pointer"
            whileHover={{ scale: 1.02 }}
          >
            <p className="font-semibold text-white mb-2">📈 Risk Scores</p>
            <p className="text-sm text-gray-400 mb-4">Ward-wise risk assessment and prediction data</p>
            <button className="text-neon-cyan text-sm font-semibold hover:text-neon-blue transition-colors">
              Export as JSON →
            </button>
          </motion.div>
          <motion.div
            className="p-6 rounded-xl bg-white/5 border border-white/10 hover:border-neon-cyan/30 transition-colors duration-300 cursor-pointer"
            whileHover={{ scale: 1.02 }}
          >
            <p className="font-semibold text-white mb-2">🎯 Hotspot Predictions</p>
            <p className="text-sm text-gray-400 mb-4">AI predictions with confidence scores and timelines</p>
            <button className="text-neon-cyan text-sm font-semibold hover:text-neon-blue transition-colors">
              Export as CSV →
            </button>
          </motion.div>
          <motion.div
            className="p-6 rounded-xl bg-white/5 border border-white/10 hover:border-neon-cyan/30 transition-colors duration-300 cursor-pointer"
            whileHover={{ scale: 1.02 }}
          >
            <p className="font-semibold text-white mb-2">🏭 Infrastructure Metrics</p>
            <p className="text-sm text-gray-400 mb-4">Complete infrastructure health and degradation data</p>
            <button className="text-neon-cyan text-sm font-semibold hover:text-neon-blue transition-colors">
              Export as Excel →
            </button>
          </motion.div>
        </div>
      </motion.div>

      {/* Scheduled Reports */}
      <motion.div
        className="glass-premium rounded-2xl p-8 border border-white/10"
        initial={{ opacity: 0, y: 20 }}
        animate={{ opacity: 1, y: 0 }}
        transition={{ delay: 0.6 }}
      >
        <h3 className="text-2xl font-bold text-white mb-6">Scheduled Reports</h3>
        <p className="text-gray-300 mb-4">Automatically generated reports delivered to your inbox</p>
        <div className="space-y-3">
          {[
            { name: 'Weekly Executive Summary', frequency: 'Every Monday 9:00 AM', status: 'Active' },
            { name: 'Monthly Risk Assessment', frequency: 'First of every month', status: 'Active' },
            { name: 'Ward Performance Report', frequency: 'Bi-weekly on Wednesdays', status: 'Inactive' },
          ].map((report, idx) => (
            <div
              key={idx}
              className="flex items-center justify-between p-4 bg-white/5 rounded-lg border border-white/5"
            >
              <div>
                <p className="font-semibold text-white">{report.name}</p>
                <p className="text-sm text-gray-400">{report.frequency}</p>
              </div>
              <div className="flex items-center gap-3">
                <span
                  className={`px-3 py-1 rounded-full text-xs font-semibold ${
                    report.status === 'Active'
                      ? 'bg-emerald-accent/20 text-emerald-accent'
                      : 'bg-gray-500/20 text-gray-400'
                  }`}
                >
                  {report.status}
                </span>
                <button className="text-gray-400 hover:text-white transition-colors">⚙️</button>
              </div>
            </div>
          ))}
        </div>
      </motion.div>
    </div>
  )
}

export default Reports
